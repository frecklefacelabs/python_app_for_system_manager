Please note that for this to work, you need to create a tag, and then use that in the .nix configuration.

Here we're creating release v1.0.0 by typing the following:

```
git tag v1.0.0
git push origin v1.0.0
```
